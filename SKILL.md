---
name: dsh-stock-analysis
description: 股票分析与买卖时机研判（A股），含多维度选股推荐与双向交易视角。用户提到「股票」「代码」「K线」「买卖时机」「该不该买/卖」「持仓盈亏」「新闻/情绪」「分析 xx 股票」「今日推荐」「选股」「止盈」「减仓」「套了」等意图时自动触发。单股分析抓取实时行情、日K线、相关新闻与基本面，计算技术指标、多因子买卖信号、未来增长评分，生成 K 线图与 HTML 分析报告，并计算持仓盈亏；选股推荐按「技术+低位+情绪+增长+动量」五维综合打分排序（screen.mjs），输出低位买入候选与高位卖出提醒。
---

# DSH 股票分析（dsh-stock-analysis）

在对话中直接给出 **K 线图 + 关键新闻 + 市场情绪 + 买卖时机 + 持仓盈亏** 的一站式股票分析。

## 触发场景

- 用户给出股票代码（600519 / sh600519 / 000001 / 301xxx）或名称（贵州茅台 / 平安银行）
- 用户问「这只股票怎么样 / 该不该买 / 要不要卖 / 现在能进吗」
- 用户提供持仓信息（「我买了 100 股，成本 1300」）要求计算盈亏
- 用户要看 K 线、新闻、情绪、支撑压力位
- 用户问「今日推荐」「有什么股票推荐」「帮我选股」「哪个能买」

## 选股推荐（screen.mjs）

用户要「今日推荐 / 选股」时，用 **五维综合打分** 从沪深榜单筛选并排序：

```bash
node <SKILL_DIR>/scripts/screen.mjs --top 10 --days 60 --out <工作区>/screen.json --html <工作区>/screen-board.html
```

**五维权重**：

| 维度 | 权重 | 说明 |
|---|---|---|
| 技术面 | 30% | 复用 analyze 信号分（均线/MACD/RSI/KDJ/量能/布林） |
| 低位买入信号 | 20% | 距60日高点回撤 + 区间位置 + 布林下轨/RSI超卖 + 低位放量 |
| 情绪面 | 15% | 新闻情绪分（利好/利空） |
| 未来增长 | 15% | 净利/营收同比 + 增长趋势 + ROE + 毛利率 + PEG |
| 动量（追高风险） | 20% | 当日涨幅 + 5/10日涨幅；**涨停/接近涨停重罚，强制标记「追高风险」** |

**回复用户**（双向交易视角）：
- **低位买入视角**：给出按综合分排序的推荐榜（含五维明细、位置研判、买卖时机、支撑/压力位），说明每只的推荐理由（低位信号/增长亮点/情绪面/追高警告）。「低位区·可低吸」的股票优先推荐。
- **高位卖出视角**：单独列出「高位卖出提醒」——卖出信号≥55 的股票（区间高位/超买/接近高点/放量滞涨）提示持仓者止盈/减仓，**不得推荐买入**。
- 涨停或当日大涨超 5% 的股票即使技术面强也要明确提示「追高风险、建议回调后再评估」。

可选 `--strategy gain|amount|volume|turnover` 切换选股池（默认 gain 涨幅榜，amount 成交额榜更不易追高）、`--min-price` / `--max-price` 过滤价格区间。数据源为新浪行情榜 + 东方财富行情/财务接口，全部免费无需 key。

## 工作流

1. **识别股票与持仓**：从用户消息中提取股票代码/名称，以及可选的「股数 + 成本价」。
2. **抓取数据**（约 3-5 秒）：
   ```bash
   node <SKILL_DIR>/scripts/fetch.mjs <代码或名称> --days 120 --out <工作区>/stock-data.json
   ```
   数据写入当前会话工作区（如 `dsh-stock-analysis/data/`），便于用户查看。
3. **技术分析 + 信号 + 情绪 + 盈亏**：
   ```bash
   node <SKILL_DIR>/scripts/analyze.mjs <data.json> [--shares N --cost P] --out <result.json>
   ```
4. **渲染 K 线图与 HTML 报告**：
   ```bash
   node <SKILL_DIR>/scripts/render.mjs <data.json> <result.json> --out <report.html> --svg <chart.svg> --summary
   ```
5. **回复用户**：
   - 直接粘贴 `--summary` 输出的 markdown 摘要（已含内嵌 SVG K 线图与关键结论）
   - 将 HTML 报告路径作为 Markdown 行内代码写出（如 `dsh-stock-analysis/data/600519-report.html`），deliverable 机制会自动显示可点击文件行
   - 持仓盈亏直接用中文讲清楚：浮盈/浮亏金额、比例、参考止损/止盈位

## 输出规范

- 对话摘要必须包含：现价与涨跌、K 线图（内嵌）、买卖时机结论（信号分+关键因子）、**位置研判（买/卖双向分）**、支撑/压力位、市场情绪、相关新闻 2-3 条、**未来增长评分**、持仓盈亏（如有）
- 当日涨幅 ≥5% 时，买卖时机结论后必须追加 **⚠ 追高风险提示**（涨停/接近涨停用「追高风险极大」措辞）
- 持仓建议联动位置研判：卖出信号 ≥55 时明确提示「止盈/减仓」，不得建议继续持有
- 摘要尾部标注**数据时效**（盘中数据 vs 收盘数据）
- HTML 报告是完整交付物：交互式盈亏计算器在报告内可直接输入股数/成本计算；含「位置研判（双向）」panel
- 附免责声明：分析仅供参考，不构成投资建议

## 注意事项

- 数据源为东方财富公开接口（免费、无需 key）；盘中数据约延迟数十秒，收盘后为当日最终数据
- 若用户给的代码解析失败，提示支持格式：6 位代码、sh/sz 前缀或中文名称
- 港股/美股暂不支持（架构已预留）；如需扩展改 fetch.mjs 数据源
- 新闻为 7x24 快讯过滤，若当天无直接相关新闻，会显示大盘快讯并标注
- 尊重用户风险偏好：明确建议是「技术参考」，不替用户做最终决策
- 脚本无第三方依赖，仅需 Node.js 18+（fetch 原生支持）
